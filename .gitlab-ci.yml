image:
  name: mikesupertrampster/packer

stages:
- tag-source

tag-source:
  stage: tag-source
  only:
  - master
  script:
    - export SEMVER=$(.scripts/gen-semver.py)
    - git describe --tags
    - echo ${SEMVER}