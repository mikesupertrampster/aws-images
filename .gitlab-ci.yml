image:
  name: mikesupertrampster/packer

stages:
- tag-source
- build

tag-source:
  stage: tag-source
  only:
  - master
  script:
    - scripts/gen-semver.py > semver
  artifacts:
    paths:
      - semver

build-ami:
  stage: build-ami
  only:
  - master
  script:
    - cat semver